apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "testj.fullname" . }}-test-connection"
  labels:
    {{- include "testj.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "testj.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
